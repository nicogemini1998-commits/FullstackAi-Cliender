# Copiar a .env y rellenar

# DB LeadUp (misma instancia PostgreSQL, diferente base de datos)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=leadup
DB_USER=fai_user
DB_PASSWORD=fai_db_2024_secure

# JWT propio de LeadUp
JWT_SECRET=leadup_secret_jwt_2024_cambia_esto
JWT_EXPIRY_HOURS=8

# FullStackAI agent runner (donde vive el flujo LeadUp)
FULLSTACKAI_URL=http://localhost:8001

# Scheduler
ENRICHMENT_CRON_HOUR=8
ENRICHMENT_SECTOR=restaurantes
ENRICHMENT_CITY=Madrid
ENRICHMENT_QTY=10
